# Notebooks

A `notebooks/` directory in a codebase is a dedicated space for storing interactive computing documents, such as Jupyter Notebooks (`.ipynb`) or similar tools. These files allow developers and data scientists to combine live, executable code, text, visualizations, and results into a single file.
