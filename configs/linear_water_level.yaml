project_name: water-level-classification
experiment_name: linear_water_level_classification
random_seed: 42

paths:
  raw_csv: data/processed/water_level_training_with_wind.csv
  dataset_metadata: data/processed/water_level_training_with_wind_metadata.json
  processed_dir: data/processed/linear_model
  preprocessor_artifact: models/linear_water_level/preprocessor.pkl
  model_checkpoint: models/linear_water_level/model.pt
  training_history_csv: reports/linear_water_level/training_history.csv
  training_summary_json: reports/linear_water_level/training_summary.json
  evaluation_json: reports/linear_water_level/evaluation.json
  predictions_csv: reports/linear_water_level/test_predictions.csv
  confusion_matrix_png: reports/linear_water_level/confusion_matrix.png
  pairplot_png: reports/global/linear_water_level_pairplot.png
  feature_hist_png: reports/global/linear_water_level_feature_histograms.png
  feature_importance_png: reports/linear_water_level/feature_importance.png
  training_curves_png: reports/linear_water_level/training_curves.png

data:
  target_column: water_level_class
  feature_columns:
    - water_level_m
    - rain_1h_sum
    - rain_3h_sum
    - rain_6h_sum
    - rain_12h_sum
    - rain_24h_sum
    - temp_mean
    - temp_delta_24h
    - thaw_flag
    - soil_saturation_index
    - pressure_hpa
    - humidity_percentage
    - wind_speed
    - wind_direction
    - wind_u
    - wind_v
  class_names:
    - low
    - high
  test_size: 0.2
  validation_size: 0.1
  split_strategy: temporal
  timestamp_column: timestamp
  validation_start: "2023-10-01"
  test_start: "2024-01-01"

preprocessing:
  strategy: minmax

feature_engineering:
  generate_lag_features: true
  lag_hours: 72
  generate_rolling_features: true
  rolling_windows: [3, 6, 12, 24]
  generate_seasonal_features: true

model:
  name: linear_classifier

training:
  hidden_dims: []
  learning_rate: 0.01
  epochs: 100
  batch_size: 32
  weight_decay: 0.0001

visualization:
  figure_dpi: 150
