# Models

place for the models, so here go all the `model.pt` files and `preprocessor.json` files regarding the model.

They should be stored in the following structure:
models/[model_name based on config.yaml used]/

- `model.pt`
- `preprocessor.json`
